<main>
  
  <div class='edit-show'>
    <p>Title:</p>
    <%= @book.title %>
  </div>
  
  <div class='edit-show'>
    <p>Body:</p>
    <%= @book.body %>
  </div>
  
</main>

  <%= link_to 'edit', edit_book_path(@book) %>
  <%= link_to 'Back', books_path %>